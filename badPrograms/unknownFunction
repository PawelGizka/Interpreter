val main = (val args: string[]): void => {
    printInt(factorial(5));
}

val fact = (val n: int): int => {
    var i = 1;
    var r = 1;
    while i < n + 1 do {
        r = r * i;
        i++;
    }

    return r;
}
