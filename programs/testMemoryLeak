//following program would crash without garbage collector
val main = (val args: string[]): void => {

    for val i = 0 to 4*10000000 do {
        val arr = new int[100000];
        val arr2 = new string[100000];
    }

    printString("end");
    val command = readString();
}