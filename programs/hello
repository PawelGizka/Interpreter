val a = "pawel"

val b = 5

val c = true

val main = (val args: string[]): void => {
    printString("hello world, my args:");
    var a = 1;
    while a < 10 do {
        printString("hi");
        //printInt(a);
        a++;
        //printInt(a);
    }

    printString("hello " + b + " " + " " + c );
    //for val arg : args do printString("arg: " + arg);
}